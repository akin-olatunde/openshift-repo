apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: new-nginx
  name: new-nginx
  namespace: olatunde-dev
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
  selector:
    matchLabels:
      app: new-nginx
  replicas: 3
  template:
    metadata:
      labels:
        app: new-nginx
    spec:
      containers:
      - image: docker.io/bitnami/nginx:latest
        imagePullPolicy: IfNotPresent
        name: nginx
        ports:
        - containerPort: 8080
          name: http
          protocol: TCP
        - containerPort: 8443
          name: https
          protocol: TCP     
