apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: stag-kustom-nginx
  namespace: openshift-gitops
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  project: default
  source:
    repoURL: 'https://github.com/akin-olatunde/openshift-repo.git'
    targetRevision: main
    path: apps/workloads/kustomize/frontend-nginx/overlays/staging
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: olatunde-staging
  syncPolicy:
    automated:
      enabled: true
      prune: true
      selfHeal: true
